# Best Practices Guide - Shared Pixel Board\n\n## 🎯 Development Guidelines\n\n### Code Organization\n\n#### ✅ DO\n\n```javascript\n// ✓ Group related functionality\nexport class LayerManager {\n  addLayer(name) { }\n  removeLayer(id) { }\n  selectLayer(id) { }\n}\n\n// ✓ Use factory functions for object creation\nexport function createLayer(name, width, height) {\n  return {\n    id: generateId(),\n    name,\n    width,\n    height,\n    pixels: new Map()\n  };\n}\n\n// ✓ Separate concerns into different modules\n// state.js - State management\n// math.js - Calculations\n// canvas.js - Rendering\n// events.js - Event handling\n```\n\n#### ❌ DON'T\n\n```javascript\n// ✗ Mix concerns in one file\nclass Everything {\n  // state management\n  // rendering\n  // events\n  // math\n}\n\n// ✗ Create objects inline\nstate.layers = [\n  { id: 1, pixels: new Map(), /* ... */ },\n  { id: 2, pixels: new Map(), /* ... */ }\n];\n\n// ✗ Tightly couple modules\nrenderer.state = state;\nstate.renderer = renderer;\n```\n\n### Error Handling\n\n#### ✅ DO\n\n```javascript\n// ✓ Validate inputs at function boundaries\nexport function drawPixel(x, y, color) {\n  if (typeof x !== 'number' || typeof y !== 'number') {\n    console.warn('Invalid coordinates');\n    return false;\n  }\n  if (!/^#[0-9A-F]{6}$/i.test(color)) {\n    console.warn('Invalid color format');\n    return false;\n  }\n  // Safe to proceed\n}\n\n// ✓ Use meaningful error messages\ntry {\n  const layer = findLayer(id);\n  if (!layer) {\n    throw new Error(`Layer not found with ID: ${id}`);\n  }\n} catch (err) {\n  console.error(`Failed to find layer:`, err.message);\n}\n\n// ✓ Provide error context\nreportError(error, {\n  operation: 'draw',\n  coordinates: { x, y },\n  color,\n  layerId\n});\n```\n\n#### ❌ DON'T\n\n```javascript\n// ✗ Silently fail\nexport function drawPixel(x, y, color) {\n  // No validation - might crash later\n}\n\n// ✗ Generic error messages\nthrow new Error('Error occurred');\n\n// ✗ Ignore exceptions\ntry {\n  riskyOperation();\n} catch (err) {\n  // Empty - hidden bug\n}\n```\n\n### Performance\n\n#### ✅ DO\n\n```javascript\n// ✓ Debounce high-frequency events\nconst handleMouseMove = debounceHandler(\n  (x, y) => state.emit('draw', { x, y }),\n  50 // milliseconds\n);\n\n// ✓ Use event delegation\ndelegator.delegate(container, '.pixel-cell', 'click', handler);\n\n// ✓ Mark state as dirty to batch updates\nstate.isDirty = true; // Render loop checks this\n\n// ✓ Clean up on unmount\nwindow.addEventListener('beforeunload', () => {\n  delegator.cleanup();\n  renderLoop.stop();\n});\n\n// ✓ Cache expensive calculations\nconst GRID_SIZES = new Map();\nfunction getGridSize(zoom) {\n  if (!GRID_SIZES.has(zoom)) {\n    GRID_SIZES.set(zoom, calculateGridSize(zoom));\n  }\n  return GRID_SIZES.get(zoom);\n}\n```\n\n#### ❌ DON'T\n\n```javascript\n// ✗ Direct listeners on high-frequency events\nmouseMoveElement.addEventListener('mousemove', (e) => {\n  state.emit('draw', { x: e.clientX, y: e.clientY });\n});\n\n// ✗ Individual listeners for many elements\ndocument.querySelectorAll('.pixel').forEach(el => {\n  el.addEventListener('click', handler); // Inefficient!\n});\n\n// ✗ Synchronous rendering on every event\nelement.addEventListener('input', () => {\n  renderer.render(); // May render many times per frame\n});\n\n// ✗ Forget cleanup\nfunction init() {\n  element.addEventListener('click', handler);\n  // No removal - memory leak!\n}\n```\n\n### State Management\n\n#### ✅ DO\n\n```javascript\n// ✓ Use immutable patterns\nconst newState = {\n  ...oldState,\n  activeLayer: layerId,\n  modified: false\n};\n\n// ✓ Emit changes for subscribers\nstate.on('layer-changed', (layerId) => {\n  ui.updateLayerDisplay(layerId);\n});\n\n// ✓ Maintain single source of truth\nclass AppState extends EventEmitter {\n  activeLayerId = null; // Single place\n  \n  setActiveLayer(id) {\n    this.activeLayerId = id;\n    this.emit('layer-changed', id);\n  }\n}\n```\n\n#### ❌ DON'T\n\n```javascript\n// ✗ Mutate state directly\nstate.activeLayer.pixels.set(key, color); // Unpredictable\n\n// ✗ Duplicate state in multiple places\nlet layerId = state.activeLayerId;\nlet alsoLayerId = ui.selectedLayer; // Out of sync!\n\n// ✗ Skip emitting changes\nstate.activeLayerId = id; // Subscribers don't know!\n```\n\n### Testing\n\n#### ✅ DO\n\n```javascript\n// ✓ Test module initialization\ntest('PixelBoardState initializes with defaults', () => {\n  const state = new PixelBoardState();\n  expect(state.layers).toBeDefined();\n  expect(state.layers.length).toBe(1);\n});\n\n// ✓ Test state transitions\ntest('Changing active layer emits event', async () => {\n  const state = new PixelBoardState();\n  let received = false;\n  state.on('layer-changed', () => { received = true; });\n  state.setActiveLayer(2);\n  expect(received).toBe(true);\n});\n\n// ✓ Test with realistic data\ntest('Drawing multiple pixels', () => {\n  const pixels = [\n    { x: 0, y: 0 },\n    { x: 1, y: 1 },\n    { x: 2, y: 2 }\n  ];\n  tools.commitPixels(pixels, '#FF0000');\n  pixels.forEach(p => {\n    expect(state.getPixel(p.x, p.y)).toBe('#FF0000');\n  });\n});\n```\n\n#### ❌ DON'T\n\n```javascript\n// ✗ Test implementation details\ntest('Internal map created', () => {\n  expect(state.pixelMap instanceof Map).toBe(true);\n});\n\n// ✗ Test multiple concerns at once\ntest('Everything works', () => {\n  // ... 100 lines of test code\n});\n\n// ✗ Skip error cases\ntest('Drawing pixels', () => {\n  // Only test happy path\n  tools.drawPixel(10, 10, '#FF0000');\n  // Never test invalid inputs\n});\n```\n\n## 🚨 Common Pitfalls\n\n### Memory Leaks\n\n```javascript\n// ❌ LEAK: Event listener never removed\nfunction setupEvents() {\n  element.addEventListener('click', handler);\n  // Handler is never removed\n}\n\n// ✅ FIX: Use delegator for cleanup\nfunction setupEvents() {\n  delegator.on(element, 'click', handler);\n  // Later:\n  delegator.cleanup(); // All removed\n}\n```\n\n### Race Conditions\n\n```javascript\n// ❌ RACE: Async operation and state mismatch\nlet layerId = state.activeLayerId;\nawait saveLayer(layerId);\nstate.activeLayer = newLayer; // layerId might be wrong!\n\n// ✅ FIX: Capture state at call time\nfunction saveActiveLayer() {\n  const layerId = state.activeLayerId;\n  return saveLayer(layerId);\n}\n```\n\n### Circular Dependencies\n\n```javascript\n// ❌ CIRCULAR: Renderer and State depend on each other\n// renderer.js\nimport { state } from './state.js';\nstate.renderer = new Renderer();\n\n// state.js\nimport { Renderer } from './renderer.js';\n\n// ✅ FIX: Inject dependencies\nclass Renderer {\n  constructor(state) {\n    this.state = state;\n  }\n}\nconst renderer = new Renderer(state);\n```\n\n## 📋 Code Review Checklist\n\n- [ ] All functions have single responsibility\n- [ ] Input validation for public APIs\n- [ ] Error handling with try-catch or validation\n- [ ] No console logging (use state.emit instead)\n- [ ] Event listeners are cleaned up\n- [ ] No circular dependencies\n- [ ] Performance-critical loops are optimized\n- [ ] State is not mutated unexpectedly\n- [ ] Comments explain WHY, not WHAT\n- [ ] Test coverage > 80%\n\n## 🎓 Learning Resources\n\n### Architecture Patterns\n- Event-driven architecture\n- Observer pattern (state.on)\n- Factory pattern (createLayer)\n- Delegation pattern (event delegation)\n\n### Performance\n- Event debouncing and throttling\n- RequestAnimationFrame and frame budgeting\n- Memory profiling\n- Canvas API optimization\n\n### Testing\n- Unit testing state changes\n- Integration testing workflows\n- Performance testing\n- Edge case identification\n"